# axi-core

Shared toolkit for AXI command-line tools.

Extracted from the duplicated toolkit layer in `ha-axi` and `plex-axi`: TOON encoding,
the errors-as-recovery-data contract, redaction, and environment configuration.

This README is a placeholder created at repository initialisation and will be replaced
by the first extraction change.
