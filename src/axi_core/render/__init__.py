"""Renderers for recovery intent.

:mod:`axi_core.render.cli` writes the line a shell user or an agent shelling out
reads; :mod:`axi_core.render.prose` writes the sentence a caller that will never run a
command reads. Both take the tool's name as an argument, because neither the error nor
the recovery knows it.
"""
