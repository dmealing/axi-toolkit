"""The generated conformance layer: the declaration's checks, and what they read.

``capture.json`` holds every expected value in this repository, machine-written by
``scripts/reqgen.py capture`` from the two source tools and the vendored specification.
``projections.py`` is what wrote it, and what reads this package for the comparison.
``relations.py`` is how the two are compared, and how each comparison is proved capable
of failing. ``test_requirements_generated.py`` is generated from
``metaobjects/meta.axi-core.yaml`` and is not edited by hand.
"""
